---
phase: 01-live-bilingual-foundation
plan: 3
type: execute
wave: 2
depends_on: [01-02]
files_modified:
  - frontend/src/app/layout.tsx
  - frontend/src/app/[locale]/layout.tsx
  - frontend/src/app/[locale]/page.tsx
  - frontend/src/app/[locale]/globals.css
  - frontend/src/components/header.tsx
  - frontend/src/components/footer.tsx
  - frontend/src/components/lang-toggle.tsx
  - frontend/src/components/theme-toggle.tsx
  - frontend/src/components/health-badge.tsx
  - frontend/src/messages/ar.json
  - frontend/src/messages/en.json
autonomous: true
requirements: [FOUND-02, FOUND-03, FOUND-04, FOUND-06, FOUND-07]
user_setup: []

must_haves:
  truths:
    # FOUND-03 / FOUND-04 core
    - "globals.css @theme declares the handoff §1 tokens verbatim for light theme plus the dark overrides, risk-band A-D chips and alert-severity (low/medium/high) chip triples with the exact hex values from the 01-UI-SPEC token tables; components consume semantic token utilities only, never raw hex (D-09, D-10, D-12)"
    - "IBM Plex Sans Arabic loads via next/font/google with subsets ['arabic','latin'] and weights 400/700 only, display swap — Arabic glyphs render in Plex with no FOUT/layout shift; weight 500 exists only as a reserved CSS custom property (FOUND-04)"
    # FOUND-02 (flash-free theme)
    - "First paint has the correct theme class on <html> resolved server-side from the theme cookie; default is light with enableSystem false so the OS preference never overrides the deterministic demo default (D-11)"
    - "Reloading resolves the same theme deterministically from the cookie, and toggling the theme twice returns to the original theme (FOUND-02 idempotency)"
    - statement: "With two tabs open, a theme toggle in one tab may leave the other tab stale until reload — accepted, no cross-tab sync required (FOUND-02 concurrency)"
      verification: backstop
    # FOUND-06 (responsive)
    - "The landing page is static and complete at every width — no data placeholders; content renders fully from 390px through desktop (FOUND-06 empty)"
    - "Layout order follows the document dir attribute: /ar renders RTL flow and /en renders mirrored LTR — driven by html dir, not per-component overrides (FOUND-06 ordering)"
    - statement: "At exactly 390px, logo wordmark + language pill + theme toggle do not overlap or clip, in ar-RTL and en-LTR, light and dark (FOUND-06 adjacency; identical to UI-SPEC E1 overflow backstop — visually checked at the 01-04 live-verify checkpoint)"
      verification: backstop
    # FOUND-07
    - "The demo-dataset disclaimer renders in the shared layout footer in both locales with the locked UI-SPEC copy, persistent and non-dismissible (D-07, FOUND-07)"
    # UI-SPEC ## UI Considerations lifts (covered rows)
    - "Health badge first render shows the neutral checking state (AR جارٍ التحقق / EN Checking…) until the initial /health fetch resolves (UI-SPEC E3 loading)"
    - "Health badge renders the offline state (AR غير متصل / EN Offline, risk-d red dot) on fetch failure or non-2xx, with the retry tooltip copy (UI-SPEC E3 error)"
    - "Health badge renders AR متصل / EN Live with a green dot on /health 200 (UI-SPEC E3 populated, D-06)"
    - "Header shows logo wordmark + language pill + theme toggle, all visible and tappable with >=44px hit areas, at every viewport >=390px, both locales, both themes (UI-SPEC E1 populated)"
    - "Header flex row wraps to two lines below ~360px; AR/EN label length differences absorb by wrap, never truncation (UI-SPEC E1 long-text)"
    - "Hero headline and subheadline wrap naturally with no truncation/ellipsis at every viewport >=390px (UI-SPEC E2 long-text)"
    - "Hero is single-column stacked flow; content wraps and the page scrolls vertically; no fixed-height containers (UI-SPEC E2 overflow)"
    - "Disclaimer banner wraps to multiple lines on narrow viewports and its height flexes (UI-SPEC E4 long-text)"
    - "Disclaimer is a full-width block that wraps, never clips, with no dismiss control (UI-SPEC E4 overflow)"
    - "How-it-works strip is a 3-up auto-fit minmax(320px,1fr) grid on desktop, stacking to 1 column at 390px (UI-SPEC E5 populated)"
    - "How-it-works grid items wrap with no fixed heights; long Arabic copy wraps inside each item (UI-SPEC E5 overflow)"
  artifacts:
    - "frontend/src/app/[locale]/globals.css (@theme tokens: brand, semantic layer, dark overrides, risk chips, severity chips, radii, type scale)"
    - "frontend/src/app/layout.tsx (minimal root passthrough) + frontend/src/app/[locale]/layout.tsx (html lang/dir, theme cookie read, ThemeProvider, next/font, header/footer composition)"
    - "frontend/src/app/[locale]/page.tsx (Arabic-first landing: hero, CTA, how-it-works strip)"
    - "frontend/src/components/header.tsx, footer.tsx, lang-toggle.tsx, theme-toggle.tsx, health-badge.tsx"
  key_links:
    - "The [locale] layout MUST read the theme cookie server-side and set the html class before paint — client-only resolution reintroduces the flash FOUND-02 forbids"
    - "next/font subsets MUST include 'arabic' — omitting it silently breaks Arabic glyphs (RESEARCH Pitfall 2), the one thing this phase cannot get wrong"
    - "health-badge fetches only process.env.NEXT_PUBLIC_API_URL + /health — a hardcoded host breaks D-04 and the 01-04 live wiring"
  prohibitions:
    - statement: "MUST NOT let the prohibited financing term (the Arabic and English words for the conventional lending charge) appear anywhere in landing/chrome copy — Murabaha framing only, per the hard product rule"
      status: unverified
    - statement: "MUST NOT let the OS system preference influence theme resolution — the deterministic light default for the judging machine must never be system-derived (D-11)"
      status: unverified
    - statement: "MUST NOT introduce any new off-4pt spacing value beyond the two documented 18px handoff exceptions without the UI-SPEC source+approval+non-render documentation path (checker sign-off condition)"
      status: unverified
---

## Phase Goal

**As a** hackathon judge, **I want to** see a polished, token-faithful, Arabic-first Rafid landing shell — flash-free theming, brand typography without font flicker, a visible demo disclaimer, and a live backend-status badge — correct from a 390px phone to desktop, **so that** the product already looks worth a fortune before any feature screen exists.

> Note (planner): user story derived from the ROADMAP Phase-1 Success Criteria (prose Goal line was not in user-story form). See 01-01 for the same note.

<objective>
Build the visible Phase-1 surface on top of the 01-02 routing/formatting foundation: the Tailwind v4 `@theme` semantic token system (handoff §1 verbatim + dark palette + risk-D + alert-severity chips per D-09/D-10/D-12), IBM Plex Sans Arabic via next/font, server-resolved flash-free theming with the light default (D-11), and the full app chrome — header with Rafid logo, language pill (D-08) and theme toggle, footer with the demo disclaimer (D-07) and health badge (D-06) — plus the token-styled hero landing page with the how-it-works strip (D-05). Everything responsive 390px→desktop (FOUND-06).

Purpose: This is the surface judges and the user evaluate Phase 1 on (D-05) — and the token/theme/font scaffold that is never retrofitted.
Output: A locally-verified, fully-themed bilingual shell ready for the 01-04 Vercel cutover.
</objective>

<execution_context>
@C:/Users/Saeed/Desktop/Amad Hackathon/Rawafid/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Saeed/Desktop/Amad Hackathon/Rawafid/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-bilingual-foundation/01-UI-SPEC.md
@.planning/phases/01-live-bilingual-foundation/01-PATTERNS.md
@.planning/phases/01-live-bilingual-foundation/01-RESEARCH.md
@.planning/phases/01-live-bilingual-foundation/01-CONTEXT.md
@RAFID_FRONTEND_HANDOFF.md
@.planning/phases/01-live-bilingual-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token system, fonts, and flash-free server-resolved theming</name>
  <files>frontend/src/app/[locale]/globals.css, frontend/src/app/layout.tsx, frontend/src/app/[locale]/layout.tsx, frontend/src/components/theme-toggle.tsx</files>
  <read_first>
    - frontend/src/app/ as left by 01-02 (current scaffold state — do not assume create-next-app defaults survived)
    - .planning/phases/01-live-bilingual-foundation/01-UI-SPEC.md — Color section (Full Semantic Token Reference light/dark, Risk Band & Alert Severity Chips tables — exact hex values, copy verbatim), Typography (Active Type Set 4 sizes / 2 weights, Reserved Tokens), Spacing Scale (8-pt scale + the two approved 18px exceptions)
    - RAFID_FRONTEND_HANDOFF.md §1 — the locked light-theme token source (colors, type, shape)
    - .planning/phases/01-live-bilingual-foundation/01-PATTERNS.md — globals.css section (@theme block shape) and [locale]/layout.tsx section (ThemeProvider constraints)
    - .planning/phases/01-live-bilingual-foundation/01-RESEARCH.md — Pattern 2 (flash-free theme), Pattern 3 (@theme), Pitfall 1 (dev-mode flash caveat), Pitfall 2 (arabic subset)
  </read_first>
  <action>
    Create frontend/src/app/[locale]/globals.css as the single stylesheet (imported once from the [locale] layout): `@import 'tailwindcss'` then an `@theme` block declaring — per D-09 — the brand tokens (--color-brand-navy, --color-brand-terra, --color-brand-purple, --color-brand-cream), the semantic layer (--color-page-bg, --color-card, --color-card-border, --color-hairline, --color-muted-text, --color-body-text), risk chips (--color-risk-a/b/c/d plus chip bg/text/border triples), alert-severity chips (low/medium/high triples sharing one chip anatomy, D-12), radii (--radius-card 18px, --radius-tile 12px, --radius-pill 999px), the type-scale custom properties (12/14/28/40px active set plus the 17px and weight-500 reserved tokens per UI-SPEC Typography), and --font-sans referencing the next/font CSS variable. Take every hex value verbatim from the UI-SPEC "Full Semantic Token Reference" and chip tables — light values are handoff §1 locked, dark values are the UI-SPEC concrete choices (D-10). Add a dark-theme override block scoped to the `.dark` root class re-assigning the semantic custom properties (page-bg, card, card-border, hairline, muted-text, body-text, lightened terra/purple accents, dark risk-d variant).

    Keep frontend/src/app/layout.tsx as a minimal root passthrough (next-intl requires the [locale] layout to own the html element). Build frontend/src/app/[locale]/layout.tsx per PATTERNS: html lang={locale} and dir (rtl for ar, ltr for en), suppressHydrationWarning, load IBM_Plex_Sans_Arabic from next/font/google with subsets ['arabic','latin'], weight ['400','700'], display 'swap', exposing a CSS variable consumed by --font-sans (FOUND-04; UI-SPEC caps Phase 1 at weights 400/700 — do not load 500/600). Read the `theme` cookie server-side via cookies() from next/headers and apply the resulting class to the html element so the first SSR paint is already correct (D-11, FOUND-02); wrap children in next-themes ThemeProvider with attribute="class", defaultTheme="light", enableSystem={false}. Compose Header, main, Footer (full components arrive in Task 2 — minimal placeholders here are acceptable to keep the build green).

    Create frontend/src/components/theme-toggle.tsx ('use client'): next-themes useTheme() toggling light/dark, which ALSO writes the `theme` cookie (path=/, SameSite=Lax, max-age ~1 year) on change so the server resolves the next first paint (D-11). Use lucide-react Sun/Moon icons (non-directional, no RTL mirroring needed per UI-SPEC). Button hit area >=44px (WCAG 2.5.5 per UI-SPEC).
  </action>
  <verify>
    <automated>test -f "frontend/src/app/[locale]/globals.css" && grep -q -- '--color-risk-d' "frontend/src/app/[locale]/globals.css" && grep -q '@theme' "frontend/src/app/[locale]/globals.css" && echo TOKENS_OK</automated>
    <automated>grep -q 'enableSystem={false}' "frontend/src/app/[locale]/layout.tsx" && grep -q 'defaultTheme="light"' "frontend/src/app/[locale]/layout.tsx" && grep -q 'suppressHydrationWarning' "frontend/src/app/[locale]/layout.tsx" && echo THEME_OK</automated>
    <automated>grep -q "subsets: \['arabic'" "frontend/src/app/[locale]/layout.tsx" && echo FONT_OK</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/app/[locale]/globals.css contains an @theme block; positive greps find `#032341` (brand navy), `#F7F2EC` (light page-bg), `#081C30` (dark page-bg), and a `--color-risk-d` declaration matching the UI-SPEC destructive values.
    - globals.css contains a `.dark` override block re-assigning --color-page-bg, --color-card, --color-card-border, --color-muted-text, --color-body-text.
    - [locale]/layout.tsx contains IBM_Plex_Sans_Arabic with subsets including 'arabic' and weights exactly ['400','700']; contains cookies() usage reading the theme cookie; contains ThemeProvider with attribute="class", defaultTheme="light", enableSystem={false}; html has suppressHydrationWarning and a dir expression keyed on locale.
    - theme-toggle.tsx contains useTheme from next-themes and a document.cookie write including `theme=` with SameSite=Lax.
    - `cd frontend && npm run build` succeeds.
  </acceptance_criteria>
  <done>The complete token system (light + dark + chips) exists in @theme, IBM Plex Sans Arabic loads with the arabic subset at 400/700, and theme resolves server-side from the cookie with a deterministic light default — FOUND-02/03/04 scaffolded.</done>
</task>

<task type="auto">
  <name>Task 2: App chrome (header/footer/toggles/health badge) + token-styled landing page</name>
  <files>frontend/src/components/header.tsx, frontend/src/components/footer.tsx, frontend/src/components/lang-toggle.tsx, frontend/src/components/health-badge.tsx, frontend/src/app/[locale]/page.tsx, frontend/src/messages/ar.json, frontend/src/messages/en.json</files>
  <read_first>
    - frontend/src/app/[locale]/layout.tsx and frontend/src/components/theme-toggle.tsx from Task 1 (current state)
    - frontend/src/messages/ar.json and en.json from 01-02 (existing catalog keys — extend, do not rewrite)
    - frontend/src/lib/format.ts from 01-02 (the only formatting entry point, D-16 — landing uses it for any placeholder figure)
    - .planning/phases/01-live-bilingual-foundation/01-UI-SPEC.md — Visual Hierarchy, Copywriting Contract (locked CTA/badge/disclaimer strings), Spacing Scale, Color "Accent reserved for" list
    - .planning/phases/01-live-bilingual-foundation/01-PATTERNS.md — lang-toggle.tsx, health-badge.tsx sections (incl. the real /health response shape from backend/app/api/routers/system.py)
    - RAFID_FRONTEND_HANDOFF.md §0 (product one-liner tone, Murabaha framing) and §10 (verified pitch stats, usable as hero/strip copy)
  </read_first>
  <action>
    Build header.tsx: Rafid logotype wordmark (text-based, brand navy/cream token, H1-scale reference per UI-SPEC), lang-toggle, theme-toggle in a flex row that wraps below ~360px; all controls >=44px hit areas. Build lang-toggle.tsx ('use client') as the pill "عربي / EN" (D-08) using next-intl's locale-aware Link/usePathname/useRouter wrappers from @/i18n/routing to swap /ar and /en while preserving the current path — cookie persistence is automatic via the next-intl middleware, no manual cookie code. Active state uses the accent per the UI-SPEC "Accent reserved for" list.

    Build health-badge.tsx ('use client') per PATTERNS: poll `${process.env.NEXT_PUBLIC_API_URL}/health` every 30 seconds (cadence is Claude's discretion per CONTEXT) with three states — checking (neutral, first render), live (green dot, AR متصل / EN Live, D-06), down (risk-d red dot, AR غير متصل / EN Offline, retry tooltip per UI-SPEC Copywriting Contract). If NEXT_PUBLIC_API_URL is undefined at build/runtime, render the down/offline state immediately — never substitute a default host (D-04; the DEPLOY-02 empty-input edge). Use lucide-react CircleCheck/CircleAlert (non-directional).

    Build footer.tsx: the persistent, non-dismissible demo-dataset disclaimer banner (D-07, FOUND-07) using the exact locked UI-SPEC strings in both catalogs, muted styling at 12px meta scale, full-width wrapping block; place the health badge in the footer beside it (per D-06 footer placement).

    Build [locale]/page.tsx as the landing page (D-05): hero with the Arabic-first headline at 28px/700 max-contrast (single focal anchor per UI-SPEC Visual Hierarchy), 14px subheadline using handoff §0 product one-liner tone, primary CTA anchor-scrolling to the how-it-works strip with the locked copy (AR "كيف يعمل رافد؟" / EN "How Rafid works") as the sole accent-filled element; then a 3-item how-it-works strip (connect → score → Murabaha financing) as an auto-fit minmax(320px,1fr) grid that stacks to 1 column at 390px (FOUND-06, handoff §1 grid rule). All copy from the message catalogs (both locales at parity); all colors via semantic token utilities only; spacing on the UI-SPEC 8-pt scale (18px only where the approved exceptions apply). If any numeric stat renders (handoff §10 values), it goes through lib/format.ts and is wrapped in a bdi element with tabular-nums (D-16).

    Extend ar.json/en.json with any keys Task 2 needs that 01-02 did not already create, keeping key-set parity and Murabaha-only framing.
  </action>
  <verify>
    <automated>grep -q 'NEXT_PUBLIC_API_URL' frontend/src/components/health-badge.tsx && echo ENV_DRIVEN_OK</automated>
    <automated>grep -rq 'بيانات تجريبية' frontend/src/messages/ar.json && grep -q 'Demo dataset' frontend/src/messages/en.json && echo DISCLAIMER_OK</automated>
    <automated>node -e "const a=require('./frontend/src/messages/ar.json'),e=require('./frontend/src/messages/en.json');const f=o=>JSON.stringify(Object.keys(o).sort());if(f(a)!==f(e))process.exit(1)" && echo PARITY_OK</automated>
  </verify>
  <acceptance_criteria>
    - header.tsx renders LangToggle and ThemeToggle; toggle/pill elements carry classes establishing >=44px hit areas (h-11/w-11/min-h-11 or equivalent).
    - health-badge.tsx contains the three-state union (checking/live/down), references process.env.NEXT_PUBLIC_API_URL, and `grep -c 'localhost' frontend/src/components/health-badge.tsx` returns 0.
    - footer.tsx renders the disclaimer via catalog keys; the banner has no dismiss/close control (no onClick-remove, no close icon).
    - page.tsx contains the hero + CTA (anchor href to the strip section) + a grid style/class using auto-fit minmax(320px,1fr).
    - ar.json and en.json key sets are identical; `grep -ci 'interest' frontend/src/messages/en.json` returns 0 and `grep -c 'فائدة' frontend/src/messages/ar.json` returns 0.
    - `grep -rInE '#[0-9A-Fa-f]{6}' frontend/src/components frontend/src/app/[locale]/page.tsx | grep -v ':[0-9]*: *//' | wc -l` returns 0 (semantic tokens only in components; hex lives in globals.css).
    - `cd frontend && npm run build` succeeds.
  </acceptance_criteria>
  <done>The full chrome + landing renders bilingual, token-styled, responsive, with the disclaimer permanent in the shared layout and the health badge env-driven — FOUND-06/07 built, D-05..D-08 implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Production-build render assertions (RTL/LTR, theme cookie, disclaimer, parity)</name>
  <files>(none — verification only; results recorded in 01-03-SUMMARY.md)</files>
  <read_first>
    - frontend/src/app/[locale]/layout.tsx, frontend/src/components/footer.tsx (what Tasks 1-2 actually wrote — the assertions target real markup)
    - .planning/phases/01-live-bilingual-foundation/01-RESEARCH.md — Pitfall 1 (no-flash guarantee is production-build behavior; dev server is not evidence)
  </read_first>
  <action>
    Run a production build and start it locally (next build + next start on a spare port), then assert against the served HTML — not the dev server (RESEARCH Pitfall 1): (1) GET /ar returns markup whose html tag carries dir="rtl" and lang="ar"; (2) GET /en carries dir="ltr" and lang="en"; (3) GET /ar with the request header `Cookie: theme=dark` returns html whose class list contains dark, and without the cookie contains the light default (FOUND-02 server resolution + idempotency: same cookie → same first paint); (4) the /ar response body contains the Arabic disclaimer string and /en contains the English one (FOUND-07); (5) the /ar body contains the hero headline from ar.json (copy parity smoke). Stop the server afterward. Record in the SUMMARY that the definitive no-flash visual confirmation happens against the real Vercel production deployment at the 01-04 checkpoint (per Pitfall 1), and that the 390px no-overlap check is the 01-04 visual backstop.
  </action>
  <verify>
    <automated>cd frontend && npm run build && (npx next start -p 3111 &) && sleep 6 && curl -fsS http://127.0.0.1:3111/ar | grep -q 'dir="rtl"' && curl -fsS http://127.0.0.1:3111/en | grep -q 'dir="ltr"' && curl -fsS -H 'Cookie: theme=dark' http://127.0.0.1:3111/ar | grep -q 'class="[^"]*dark' && curl -fsS http://127.0.0.1:3111/ar | grep -q 'بيانات تجريبية' && echo RENDER_OK</automated>
  </verify>
  <acceptance_criteria>
    - Served /ar HTML contains dir="rtl" lang="ar"; served /en HTML contains dir="ltr" lang="en".
    - Served /ar with Cookie theme=dark has the dark class on html; without the cookie it does not (light default).
    - Served /ar contains the Arabic disclaimer string; served /en contains the English disclaimer string.
    - All assertions ran against a production build (next build + next start), not next dev.
  </acceptance_criteria>
  <done>The shell provably renders RTL/LTR by route, resolves theme server-side from the cookie with a light default, and shows the disclaimer in both locales — verified against a production build.</done>
</task>

</tasks>

<flagged_assumptions>
Edge-probe rows accounted here (never silently dropped):

- **FOUND-03 (unclassified):** No edge shape inferable from the requirement text; the requirement (handoff §1 tokens + risk-D + severity scales in both themes) is covered by the Task-1 token truths/acceptance. Assumption: exact dark hex and the risk-D oklch-derived red are Claude's discretion per D-10/D-12 (RESEARCH A2) — the UI-SPEC concrete values are adopted, with WCAG AA contrast confirmed at implementation.
- **FOUND-04 (unclassified):** Covered by the Task-1 font truth/acceptance. Assumption: IBM Plex Sans Arabic weight availability (400/700) per Google Fonts is tertiary-confidence in RESEARCH — next/font fails at build time if a weight is unavailable, which is the loud failure mode we want.
- **FOUND-07 (unclassified):** Covered by the disclaimer truth + Task-3 render assertion; the parent requirement is fully covered by phase success criterion 4.
- **UI-SPEC planner note (explicit assumption):** collections/lists first render in Phase 3 — zero-one-many, empty-state, and pagination categories are deliberately out of Phase-1 scope and are NOT attempted here. The 8 UI-SPEC dismissed rows (E1 and E5 data-state categories) carry their recorded reasons in 01-UI-SPEC.md and need no lift.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> rendered shell | Static SSR content + two preference cookies; no user data input in Phase 1 |
| health badge -> backend /health | Client-side cross-origin fetch to the deployed API |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | npm installs | low | accept | No new package installs in this plan (lucide-react was audited + installed under the 01-02 legitimacy checkpoint) |
| T-03-02 | Information Disclosure / Tampering | theme + locale cookies | low | mitigate | Preference-only cookies (no session data), SameSite=Lax, values validated against fixed enums (ar/en, light/dark) by next-intl/next-themes |
| T-03-03 | Tampering (XSS) | message catalog rendering | low | mitigate | next-intl renders strings as text; no dangerouslySetInnerHTML anywhere in the shell |
</threat_model>

## Artifacts this phase produces

- **Files:** `frontend/src/app/[locale]/globals.css`, `frontend/src/app/layout.tsx`, `frontend/src/app/[locale]/layout.tsx`, `frontend/src/app/[locale]/page.tsx`, `frontend/src/components/header.tsx`, `footer.tsx`, `lang-toggle.tsx`, `theme-toggle.tsx`, `health-badge.tsx`
- **Components (exports):** `Header`, `Footer`, `LangToggle`, `ThemeToggle`, `HealthBadge`
- **CSS custom properties / tokens:** `--color-brand-navy/terra/purple/cream`, `--color-page-bg`, `--color-card`, `--color-card-border`, `--color-hairline`, `--color-muted-text`, `--color-body-text`, `--color-risk-a/b/c/d` + chip triples, alert-severity low/medium/high chip triples, `--radius-card/tile/pill`, type-scale properties (12/14/28/40px active + 17px/500 reserved), `--font-sans`
- **Cookies:** `theme` (light/dark, set by ThemeToggle, read server-side), next-intl locale cookie (set by middleware from 01-02)
- **Message catalog keys:** hero/CTA/how-it-works/badge-state/disclaimer keys extended in `ar.json`/`en.json`

<verification>
- Production-build render assertions (Task 3) all pass: dir/lang per locale, dark class from cookie, disclaimer in both locales.
- No raw hex in components (comment-filtered grep, Task 2 acceptance); prohibited-term greps on both catalogs return 0.
- `npm run build` green.
</verification>

<success_criteria>
The deployed-ready shell shows a token-faithful Arabic-first landing with flash-free cookie-resolved theming (light default), IBM Plex Sans Arabic without FOUT, the risk-D/severity-extended token system in both themes, the permanent bilingual disclaimer, and an env-driven three-state health badge — responsive 390px→desktop.
</success_criteria>

<output>
Create `.planning/phases/01-live-bilingual-foundation/01-03-SUMMARY.md` when done — record the final dark-palette hex choices, the risk-D value, any catalog keys added, and the note that no-flash + 390px visual confirmation completes at the 01-04 checkpoint.
</output>
