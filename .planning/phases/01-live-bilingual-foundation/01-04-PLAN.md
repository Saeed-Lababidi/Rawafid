---
phase: 01-live-bilingual-foundation
plan: 4
type: execute
wave: 3
depends_on: [01-01, 01-03]
files_modified: []
autonomous: false
requirements: [DEPLOY-02, FOUND-02, FOUND-06]
user_setup:
  - service: vercel
    why: "Cut the existing Vercel project over to the new frontend/ shell and wire it to the deployed backend (DEPLOY-02, D-01, D-02)"
    env_vars:
      - name: NEXT_PUBLIC_API_URL
        source: "The live Railway backend URL recorded in 01-01-SUMMARY.md — set for Production AND Preview environments"
    dashboard_config:
      - task: "Set project Root Directory to frontend/ (D-01); leave 'Include files outside Root Directory' OFF (RESEARCH Open Q1)"
        location: "Vercel -> Project -> Settings -> Build & Development"
      - task: "Confirm push-to-main auto-deploys to Production (D-03) and trigger the cutover deploy (D-02)"
        location: "Vercel -> Project -> Deployments"

must_haves:
  truths:
    - "The production Vercel URL serves the new Next.js shell — the D-02 cutover is complete; the old prototype HTML files remain in the repo as reference only and are never served"
    - "NEXT_PUBLIC_API_URL is set in Vercel (Production + Preview) to the deployed Railway URL; a missing NEXT_PUBLIC_API_URL fails loudly (the badge renders its offline/error state) and never silently falls back to a local host (DEPLOY-02 empty)"
    - "The health badge on the deployed production URL shows the Live state — proving env wiring + CORS end-to-end against the free-hosted backend (D-06, phase success criterion 5)"
    - "Hard refresh of the deployed production URL shows no flash of wrong theme in either theme state (FOUND-02 verified against the production deployment per RESEARCH Pitfall 1)"
    - "The deployed /ar renders Arabic RTL by default and /en renders LTR with copy parity, on the real production URL"
  artifacts:
    - "Live production Vercel deployment serving frontend/ (root-directory cutover)"
    - "Vercel env var NEXT_PUBLIC_API_URL (Production + Preview scopes)"
  key_links:
    - "Vercel Root Directory MUST be frontend/ — with it unset the build fails or serves the wrong artifact (D-01)"
    - "NEXT_PUBLIC_API_URL value MUST exactly match the live Railway URL from 01-01-SUMMARY, and the backend CORS allow-list (01-01) MUST contain this Vercel production origin — the badge only shows Live when both halves agree"
  prohibitions:
    - statement: "MUST NOT widen the backend CORS allow-list back toward a wildcard to fix a connectivity failure — fix the origin/env value instead (the narrowed allow-list is a Phase-1 security deliverable)"
      status: unverified
    - statement: "MUST NOT hardcode any backend host into frontend source to make the deploy work — the API base stays env-driven only (D-04)"
      status: unverified
---

## Phase Goal

**As a** hackathon judge, **I want to** open the real production URL on any device and experience the live Arabic-first Rafid shell talking to its live backend, **so that** everything demoed on July 17 — and everything built in Phases 2-5 — runs against real, verified infrastructure.

> Note (planner): user story derived from the ROADMAP Phase-1 Success Criteria (prose Goal line was not in user-story form). See 01-01 for the same note.

<objective>
Cut the existing Vercel project over to `frontend/` (D-01, D-02), wire `NEXT_PUBLIC_API_URL` to the live Railway backend from 01-01 (D-04), and verify the walking skeleton end-to-end on the real URLs: the shell serves bilingually, the theme is flash-free on the production build, and the health badge shows Live — closing the frontend half of DEPLOY-02 (the CORS half landed in 01-01) and the phase's live-infrastructure success criterion.

Purpose: The walking skeleton is only "walking" when the deployed frontend provably reaches the deployed backend on the public internet.
Output: The live production URL serving the shell with a green Live badge; visual confirmations recorded.
</objective>

<execution_context>
@C:/Users/Saeed/Desktop/Amad Hackathon/Rawafid/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Saeed/Desktop/Amad Hackathon/Rawafid/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-bilingual-foundation/01-CONTEXT.md
@.planning/phases/01-live-bilingual-foundation/01-RESEARCH.md
@.planning/phases/01-live-bilingual-foundation/01-01-SUMMARY.md
@.planning/phases/01-live-bilingual-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Vercel project cutover (Root Directory, env var, production deploy)</name>
  <action>Human sets the Vercel project Root Directory to frontend/ (D-01), adds NEXT_PUBLIC_API_URL for Production + Preview pointing at the Railway URL from 01-01-SUMMARY (D-04), confirms push-to-main auto-deploy (D-03), and triggers the production cutover deploy (D-02) — following the numbered steps below. Dashboard-only configuration with no CLI path on the existing project.</action>
  <what-built>01-03 produced the complete, locally-verified bilingual shell; 01-01 produced the live Railway backend with the Vercel origin CORS-allow-listed. The Vercel project cutover is dashboard-only configuration a human must perform.</what-built>
  <how-to-verify>
    1. In Vercel -> Project -> Settings -> Build & Development: set Root Directory to `frontend/` (D-01). Leave "Include files outside Root Directory" OFF (RESEARCH Open Q1 recommendation).
    2. In Settings -> Environment Variables: add NEXT_PUBLIC_API_URL for Production AND Preview, value = the exact Railway URL recorded in 01-01-SUMMARY.md (https scheme, no trailing slash).
    3. Confirm the Git integration auto-deploys pushes to main to Production (D-03), then trigger a production deploy of current main (D-02 cutover — the production URL now serves the new shell, replacing anything previously served).
    4. Confirm the production domain shown in Vercel matches the origin that 01-01 placed in the backend CORS allow-list; if it differs, update the backend env/code allow-list to the real domain and redeploy the backend.
    5. Open the production URL once — the shell should load (badge state checked in the next tasks).
  </how-to-verify>
  <resume-signal>Type "deployed <vercel-production-url>" once the production deployment is live, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Automated live-URL verification (routing, env wiring, CORS pairing)</name>
  <files>(none — live verification only; results recorded in 01-04-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/01-live-bilingual-foundation/01-01-SUMMARY.md (live Railway URL, which reset path is live, CORS origin recorded)
    - .planning/phases/01-live-bilingual-foundation/01-RESEARCH.md — Code Examples (Vercel env scoping), Assumption A3 (preview-origin CORS)
  </read_first>
  <action>
    With VERCEL_URL = the production URL from the checkpoint and RAILWAY_URL from 01-01-SUMMARY: (1) assert GET VERCEL_URL redirects to /ar and GET VERCEL_URL/ar returns 200 HTML containing dir="rtl", and /en contains dir="ltr" (FOUND-01 render on the live URL); (2) assert GET RAILWAY_URL/health with request header Origin: VERCEL_URL returns 200 and an access-control-allow-origin response header matching that origin — the exact cross-origin pair the health badge exercises (D-06, DEPLOY-02); (3) assert the served /ar HTML contains the Arabic disclaimer string (FOUND-07 live); (4) confirm the served HTML/JS references the Railway host (env baked at build time) — e.g. the Railway hostname appears in the built client payload — proving D-04 env wiring rather than a stale default. Record all outputs in the SUMMARY, plus the prod-freeze reminder: informally freeze main by end of July 16, hotfix via verified Vercel previews only (CONTEXT Discretion).
  </action>
  <verify>
    <automated>curl -fsSL "$VERCEL_URL/ar" | grep -q 'dir="rtl"' && curl -fsSL "$VERCEL_URL/en" | grep -q 'dir="ltr"' && echo LIVE_ROUTING_OK</automated>
    <automated>curl -fsS -H "Origin: $VERCEL_URL" -D - "$RAILWAY_URL/health" -o /dev/null | grep -i 'access-control-allow-origin' && echo CORS_PAIR_OK</automated>
    <automated>curl -fsSL "$VERCEL_URL/ar" | grep -q 'بيانات تجريبية' && echo DISCLAIMER_LIVE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Live /ar HTML contains dir="rtl"; live /en HTML contains dir="ltr" (production URL, not preview).
    - curl of RAILWAY_URL/health with the Vercel Origin header returns 200 and access-control-allow-origin echoing the Vercel origin.
    - Live /ar HTML contains the Arabic disclaimer string.
    - The Railway hostname is present in the deployed client payload (env-driven URL baked at build, no stale default).
  </acceptance_criteria>
  <done>The deployed frontend and deployed backend provably pair on the public internet: routing, env wiring, CORS, and disclaimer all asserted against the live URLs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification on the live production URL (no-flash, badge Live, 390px, font)</name>
  <action>Human performs the four visual-only checks on the deployed production URL — theme no-flash on hard refresh (FOUND-02, production-build-only evidence per RESEARCH Pitfall 1), health badge Live state (D-06), the 390px header no-overlap backstop in 4 locale×theme combinations (FOUND-06 / UI-SPEC E1), and Plex-Arabic font rendering (FOUND-04) — following the numbered steps below.</action>
  <what-built>The complete walking skeleton is live: production Vercel URL serving the bilingual themed shell against the Railway backend. Three remaining checks are visual-only and cannot be asserted by curl.</what-built>
  <how-to-verify>
    1. No-flash (FOUND-02): on the production URL, toggle to dark mode, then hard-refresh (Ctrl+Shift+R) — the page must paint dark immediately with no light flash. Toggle back to light, hard-refresh — no dark flash. (Production build is the only valid environment for this check per RESEARCH Pitfall 1.)
    2. Health badge (D-06): the footer badge shows "متصل" (AR) / "Live" (EN) with a green dot within ~30s of load.
    3. 390px backstop (FOUND-06 / UI-SPEC E1 overflow): in devtools responsive mode at exactly 390px width, check the header — logo wordmark, language pill, theme toggle must not overlap or clip — in all four combinations: /ar + /en, light + dark. Also confirm Arabic text renders in IBM Plex Sans Arabic (not a fallback serif) with no font flicker on reload (FOUND-04).
    4. Copy check: no occurrence of the conventional-lending term anywhere on the page in either language (Murabaha framing only).
  </how-to-verify>
  <resume-signal>Type "approved" if all four checks pass, or describe exactly which check failed and how.</resume-signal>
</task>

</tasks>

<flagged_assumptions>
Edge-probe rows accounted here (never silently dropped):

- **DEPLOY-02 ordering (flagged assumption):** "types regenerated against deployed openapi.json" is the one DEPLOY-02 clause NOT done in this phase — OpenAPI type codegen is Phase 2 scope per ROADMAP plan 02-01 (the typed client does not exist yet in Phase 1). Deliberate sequencing, not a gap.
- **DEPLOY-02 empty (covered):** lifted as the NEXT_PUBLIC_API_URL fail-loud truth above; the badge's no-default-host behavior was implemented in 01-03 Task 2 and is verified live here.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> Vercel production URL | Anyone can load the shell; static content + preference cookies only |
| Vercel-served client -> Railway backend | The cross-origin fetch pair guarded by the 01-01 CORS allow-list |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing / Information Disclosure | CORS pairing (Vercel origin <-> Railway allow-list) | high | mitigate | Task 2 asserts the exact allow-origin echo live; the prohibition forbids re-widening to wildcard as a "fix" |
| T-04-02 | Information Disclosure | NEXT_PUBLIC_API_URL in the client bundle | low | accept | Public, non-secret backend URL by design; no secret ever enters a NEXT_PUBLIC_ variable |
| T-04-SC | Tampering | package installs | low | accept | This plan installs nothing — configuration and verification only |
</threat_model>

## Artifacts this phase produces

- **Hosting configuration:** Vercel Root Directory = `frontend/`; "Include files outside Root Directory" OFF; Git auto-deploy main -> Production
- **Env vars:** `NEXT_PUBLIC_API_URL` in Vercel (Production + Preview scopes)
- **Live URLs:** the production Vercel URL serving the shell (the demo URL for judging); paired with the Railway backend URL from 01-01
- **No repo files modified** — this plan is provisioning + verification (any CORS-origin correction happens via the 01-01-owned backend files/env)

<verification>
- Live routing assertions (/ar RTL, /en LTR) pass on the production URL.
- CORS pair curl returns the Vercel origin in access-control-allow-origin.
- Human checkpoint confirms: no theme flash on production hard refresh, badge Live, 390px no-overlap in 4 combinations, Plex Arabic rendering.
</verification>

<success_criteria>
The full walking skeleton is live and verified: a judge can load the production Vercel URL, see the Arabic-first flash-free shell, and watch the health badge confirm the free-hosted backend + Postgres are reachable — every later phase now builds against these real URLs.
</success_criteria>

<output>
Create `.planning/phases/01-live-bilingual-foundation/01-04-SUMMARY.md` when done — record the production Vercel URL, the paired Railway URL, the visual-check outcomes, and the July-16 main-freeze reminder.
</output>
