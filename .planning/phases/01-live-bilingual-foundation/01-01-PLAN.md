---
phase: 01-live-bilingual-foundation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - backend.Dockerfile
  - backend/app/main.py
  - backend/app/db.py
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03]
user_setup:
  - service: railway
    why: "Free-tier compute host for the existing FastAPI backend (no-default-sleep, DEPLOY-01)"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> project -> Connection string (strip sslmode/channel_binding query params before pasting)"
      - name: JWT_SECRET
        source: "Generate fresh (e.g. openssl rand -hex 32) — never the dev default from backend/app/config.py"
      - name: FERNET_KEY
        source: "Generate fresh Fernet key (cryptography.fernet.Fernet.generate_key()) — never the dev default"
      - name: PROVIDER
        source: "Set to mock"
      - name: SCORING_BACKEND
        source: "Set to module (uses rafid-engine)"
      - name: MONITOR_ENABLED
        source: "Set to true (15s = 1 simulated day, per CONTEXT.md Discretion)"
    dashboard_config:
      - task: "Set service Root Directory unset (repo root) and RAILWAY_DOCKERFILE_PATH=backend.Dockerfile"
        location: "Railway -> Service -> Settings -> Build"
      - task: "Confirm sleep/Serverless is OFF (default) so there is no cold-start during judging"
        location: "Railway -> Service -> Settings"
      - task: "Provision TODAY (2026-07-15) so the 30-day trial window fully covers July 17; set a budget alert"
        location: "Railway -> Project -> Usage"
  - service: neon
    why: "Free-tier managed Postgres (no expiry-on-inactivity, TLS-enforced) for the deployed backend"
    dashboard_config:
      - task: "Create Postgres project; copy the pooled connection string into Railway DATABASE_URL"
        location: "Neon Console -> Connection Details"
      - task: "Confirm the host is a neon.tech host (never Render's own free Postgres — it expires in 30 days)"
        location: "Neon Console"
  - service: vercel
    why: "The production Vercel origin is needed to set the CORS allow-list on the backend (DEPLOY-02)"
    dashboard_config:
      - task: "Provide the existing project's stable production domain (https://<project>.vercel.app) for the CORS allow-list"
        location: "Vercel -> Project -> Settings -> Domains"

must_haves:
  truths:
    - "GET https://<railway-app>/health returns HTTP 200 with JSON {status:'ok', app, env, provider, scoring_backend} sourced from the live Neon-backed backend"
    - "The deployed backend connects to Neon over TLS (asyncpg connect_args ssl=require) and does not crash-loop on startup"
    - "CORS on the deployed backend allows exactly the production Vercel origin and rejects an arbitrary Origin (curl-testable) — the previous allow_origins=[\"*\"] is gone"
    - "Demo data can be reset remotely on the deployed backend (Railway shell `railway run make reset`, or a token-guarded reset path if shell exec is unavailable on the trial tier)"
    - "DATABASE_URL points at a neon.tech host, JWT_SECRET and FERNET_KEY are freshly generated for prod, and none are committed to the repo"
    - statement: "Neon free-tier connection pool holds under Phase-3 demo polling load (5-15s refetch across a few concurrent judge sessions) without connection-exhaustion errors"
      verification: backstop
  artifacts:
    - "backend.Dockerfile (repo-root build context, resolves the ../rafid-engine path dependency, uses uv.lock)"
    - "backend/app/main.py CORS allow_origins narrowed to the Vercel origin"
    - "backend/app/db.py _engine_kwargs non-sqlite branch adds connect_args ssl=require"
    - "A live backend base URL (Railway) reachable over HTTPS"
    - "A live Neon Postgres instance"
  key_links:
    - "backend.Dockerfile build context MUST be the repo root (not backend/) or the ../rafid-engine path dependency fails at uv sync"
    - "DATABASE_URL must have sslmode/channel_binding query params stripped (asyncpg rejects sslmode kwarg) AND the code must pass ssl=require via connect_args"
    - "CORS allow_origins value must match the exact scheme+host of the deployed Vercel production URL or the health badge (01-03) shows Offline"
  prohibitions:
    - statement: "MUST NOT ship the dev-default JWT_SECRET or FERNET_KEY (the hardcoded fallbacks in backend/app/config.py) to the deployed backend"
      status: unverified
    - statement: "MUST NOT provision any paid hosting resource — zero-budget constraint; Railway trial credit + Neon free tier + existing Vercel project only"
      status: unverified
    - statement: "MUST NOT expose a demo-reset path that is callable without a guard (host-console exec, or a static bearer token read from an env var that is never logged or committed)"
      status: unverified
---

## Phase Goal

**As a** hackathon judge (and the demo operator), **I want to** reach a live, free-hosted Rafid backend and Postgres from the public internet with no cold-start risk and a remote data-reset path, **so that** every later Rafid screen is built and demoed against real deployed infrastructure before July 17.

> Note (planner): the ROADMAP `**Goal:**` line for Phase 1 is in prose form, not the canonical `As a / I want to / So that` user-story shape. This story is derived faithfully from the ROADMAP Phase-1 Success Criteria (which are written from the user's perspective), not invented. If a stricter story is wanted, run `/gsd mvp-phase 1` to set it on the ROADMAP first.

<objective>
Take the existing, complete FastAPI backend + `rafid-engine` and stand it up on a free host (Railway primary) against free-tier Neon Postgres, reachable over HTTPS from the production Vercel origin with the CORS allow-list narrowed, TLS wired for asyncpg, and a remote demo-reset path proven. This is the highest-risk, most time-sensitive half of Phase 1 (RESEARCH: the real risk lives in deploy plumbing, and the trial window is time-bound against July 17) — it runs in Wave 1 in parallel with the frontend foundation (01-02) because it shares no files with `frontend/`.

Purpose: Prove live infrastructure now so the walking-skeleton health badge (built in 01-03, wired live in 01-04) can show "Live" and Phases 2-4 build against a real URL.
Output: A deployed backend base URL, a live Neon DB, three minimal deploy-config code edits, and a verified remote-reset path.
</objective>

<execution_context>
@C:/Users/Saeed/Desktop/Amad Hackathon/Rawafid/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Saeed/Desktop/Amad Hackathon/Rawafid/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-bilingual-foundation/01-RESEARCH.md
@.planning/phases/01-live-bilingual-foundation/01-PATTERNS.md
@.planning/phases/01-live-bilingual-foundation/01-CONTEXT.md
@backend/app/main.py
@backend/app/db.py
@backend/Dockerfile
@backend/Makefile
@backend/app/api/routers/system.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Minimal deploy-config code edits (repo-root Dockerfile, CORS allow-list, asyncpg TLS)</name>
  <files>backend.Dockerfile, backend/app/main.py, backend/app/db.py</files>
  <read_first>
    - backend/Dockerfile (current 12-line single-stage build, context=backend/ — structurally cannot see sibling rafid-engine/)
    - backend/app/main.py (CORS block currently lines 48-55, allow_origins=["*"])
    - backend/app/db.py (_engine_kwargs currently lines 14-18, non-sqlite branch returns {"pool_pre_ping": True})
    - backend/pyproject.toml (has [tool.uv.sources] rafid-engine = { path = "../rafid-engine" }; note uv.lock exists)
    - rafid-engine/pyproject.toml (confirms the sibling package resolved by the path dependency)
    - .planning/phases/01-live-bilingual-foundation/01-RESEARCH.md — Pattern 5 (repo-root Docker build context) and Pitfall 3 (asyncpg + Neon SSL)
    - .planning/phases/01-live-bilingual-foundation/01-PATTERNS.md — the three "backend edit" sections (exact current-code + target-state for Dockerfile, main.py, db.py)
  </read_first>
  <action>
    Create a NEW repo-root file `backend.Dockerfile` (do NOT modify or delete the existing backend/Dockerfile or backend/docker-compose.yml — both stay for local dev) implementing the repo-root build-context pattern from 01-PATTERNS.md / RESEARCH.md Pattern 5: base python:3.12-slim, WORKDIR /srv, COPY rafid-engine into ./rafid-engine, COPY backend/pyproject.toml + backend/uv.lock into ./backend/, WORKDIR /srv/backend, install uv then `uv sync --frozen --no-dev` (uses the lockfile — the current Dockerfile's `uv pip install -r pyproject.toml` does not), COPY backend source, EXPOSE 8000, CMD runs `uv run uvicorn app.main:app --host 0.0.0.0 --port 8000`. The build context MUST be the repo root so ../rafid-engine resolves.

    In backend/app/main.py, change ONLY the CORS middleware `allow_origins` argument: replace `["*"]` with the production Vercel origin. Set `allow_origins` to a list holding the stable production domain `https://<PROJECT>.vercel.app` (exact domain from Vercel user_setup); per RESEARCH A3 and D-03 (push-to-main auto-deploys, previews tested), ALSO add `allow_origin_regex=r"https://.*\.vercel\.app$"` so preview deployments reach the backend during the sprint. Leave allow_credentials=False and allow_methods/allow_headers=["*"] unchanged. Do not touch router registration or the lifespan block.

    In backend/app/db.py, change ONLY the non-sqlite return of `_engine_kwargs` to add `"connect_args": {"ssl": "require"}` alongside the existing `"pool_pre_ping": True` (per RESEARCH Pitfall 3 — required for Neon/asyncpg, not optional). The sqlite branch is unchanged. The Neon connection string's `sslmode=`/`channel_binding=` query params are stripped at the Railway env-var level (Task 2), not in code.

    Generate fresh production secrets locally for later paste into Railway env (Task 2): a JWT_SECRET (32-byte hex) and a FERNET_KEY (Fernet.generate_key()). These are NEVER committed and NEVER written into any tracked file — they exist only to be pasted into the Railway dashboard. Do not reuse the dev defaults from backend/app/config.py.
  </action>
  <verify>
    <automated>test -f backend.Dockerfile && grep -q 'rafid-engine' backend.Dockerfile && grep -q 'uv sync --frozen' backend.Dockerfile && echo DOCKERFILE_OK</automated>
    <automated>grep -n 'connect_args' backend/app/db.py | grep -q 'ssl' && echo SSL_OK</automated>
    <automated>grep -n 'allow_origins' backend/app/main.py | grep -vq '"\*"' && grep -q 'vercel' backend/app/main.py && echo CORS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Repo-root `backend.Dockerfile` exists; contains a COPY of rafid-engine and uses `uv sync --frozen`.
    - `backend/Dockerfile` and `backend/docker-compose.yml` are byte-unchanged (only a NEW file was added at repo root).
    - `backend/app/db.py` non-sqlite branch contains `"connect_args": {"ssl": "require"}`; sqlite branch unchanged.
    - `backend/app/main.py` CORS `allow_origins` no longer contains the bare `"*"` and references a `.vercel.app` origin; allow_credentials still False.
    - No JWT_SECRET or FERNET_KEY value appears in any tracked/committed file (git diff shows no secret literals).
  </acceptance_criteria>
  <done>The three minimal deploy-config edits exist, the repo-root Dockerfile resolves the monorepo path dependency, and fresh prod secrets are ready for the Railway dashboard (uncommitted).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Provision Railway + Neon and deploy the backend (human dashboard actions)</name>
  <action>Human provisions Neon Postgres and the Railway service (repo-root build context via RAILWAY_DOCKERFILE_PATH=backend.Dockerfile), sets the production env vars (stripped Neon DATABASE_URL, fresh JWT_SECRET/FERNET_KEY, PROVIDER=mock, SCORING_BACKEND=module, MONITOR_ENABLED=true), and deploys — following the numbered steps below. Claude cannot perform fresh-account signups or trial-tier dashboard configuration.</action>
  <what-built>Repo-root Dockerfile + CORS/TLS code edits from Task 1 are ready; the backend now needs a live host and DB that only a human can provision (account signups + dashboard config have no CLI-only path on a fresh trial).</what-built>
  <how-to-verify>
    1. Create a Neon project; copy the pooled connection string. STRIP the `?sslmode=require&channel_binding=require` (and any other) query params from it before using it as DATABASE_URL (asyncpg rejects the sslmode kwarg; the code supplies ssl=require). Confirm the host ends in `neon.tech`.
    2. Create a Railway project pointed at this repo. In Service -> Settings -> Build: leave Root Directory unset (repo root) and set `RAILWAY_DOCKERFILE_PATH=backend.Dockerfile`. Confirm sleep/Serverless is OFF.
    3. Set Railway service variables: DATABASE_URL (stripped Neon URL), JWT_SECRET (fresh), FERNET_KEY (fresh), PROVIDER=mock, SCORING_BACKEND=module, MONITOR_ENABLED=true, plus the fee-schedule defaults if the backend requires them (see backend/.env.example / STACK.md Configuration).
    4. Deploy. Watch the Railway build log: it must complete `uv sync --frozen` WITHOUT a `../rafid-engine does not exist` error, and the container must start uvicorn without a crash-loop mentioning `sslmode`/SSL.
    5. Note the assigned Railway HTTPS URL (e.g. https://<app>.up.railway.app) — record it; 01-04 (Vercel cutover) needs it as NEXT_PUBLIC_API_URL.
  </how-to-verify>
  <resume-signal>Type "deployed <railway-url>" once the build is green and the URL is live, or paste the build/runtime error if it failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify live backend health, no-cold-start, and the remote demo-reset path</name>
  <files>(none — live verification only; results recorded in 01-01-SUMMARY.md)</files>
  <read_first>
    - backend/app/api/routers/system.py (the /health endpoint shape the badge and this check assert against)
    - backend/Makefile (the `reset` target = `uv run python -m app.seed.run --reset` — the remote-reset path must reach this)
    - .planning/phases/01-live-bilingual-foundation/01-RESEARCH.md — Open Questions #3 (Railway exec/shell availability on trial tier) and Environment Availability
    - .planning/STATE.md — Blockers (Railway credit / Neon expiry / remote-reset-without-SSH, re-verify at deploy time)
  </read_first>
  <action>
    Against the live Railway URL from the checkpoint: confirm GET /health returns 200 with the expected JSON keys (status, app, env, provider, scoring_backend) and that env/provider/scoring_backend reflect the prod env vars (provider=mock, scoring_backend=module). Confirm a second /health call moments later still returns 200 (no spin-down between requests — Railway no-default-sleep).

    Confirm the CORS narrowing works: a curl with `-H "Origin: https://<project>.vercel.app"` returns an `access-control-allow-origin` header echoing that origin, and a curl with `-H "Origin: https://evil.example.com"` does NOT return that origin in the allow header.

    Verify the remote demo-reset path (DEPLOY-03): first try the host-console path — `railway run make reset` (or the Railway web shell running `python -m app.seed.run --reset`) against the deployed service — and confirm it re-seeds without error. If Railway's trial tier does NOT permit shell/exec against the deployed service, fall back to the smallest-possible backend delta: a token-guarded reset path that calls the existing `app.seed.run --reset` logic, gated behind a static bearer token read from an env var (never logged, never committed) — this is the only additional sanctioned backend change and only if the console path is unavailable. Record which path is live.

    Record in the SUMMARY: the provision date vs July 17 (trial window margin), that DATABASE_URL is a neon.tech host, and confirm the Render+UptimeRobot fallback remains documented-only (not provisioned) per the assumption-delta decision below.
  </action>
  <verify>
    <automated>curl -fsS "$RAILWAY_URL/health" | grep -q '"status"' && echo HEALTH_OK</automated>
    <automated>curl -fsS -H "Origin: https://evil.example.com" -D - "$RAILWAY_URL/health" -o /dev/null | grep -i 'access-control-allow-origin' | grep -vq 'evil.example.com' && echo CORS_REJECTS_OTHER || echo CORS_REVIEW</automated>
  </verify>
  <acceptance_criteria>
    - `curl $RAILWAY_URL/health` returns 200 and JSON containing `"status":"ok"`, `"provider":"mock"`, `"scoring_backend":"module"`.
    - Two consecutive /health calls both return 200 with no multi-second wake delay (no-cold-start evidence).
    - curl with a Vercel Origin header returns access-control-allow-origin for that origin; curl with a foreign Origin does not receive that foreign origin in the allow header.
    - The remote demo-reset path (console exec OR token-guarded path) has been executed once and re-seeded the DB without error; which path is live is recorded in the SUMMARY.
    - SUMMARY records: Neon host confirmed neon.tech, trial-window margin vs July 17, secrets are fresh + uncommitted.
  </acceptance_criteria>
  <done>The backend is provably live on free infrastructure, CORS-narrowed, TLS-connected to Neon, no-cold-start, and remotely resettable — DEPLOY-01/DEPLOY-03 satisfied and the CORS half of DEPLOY-02 in place.</done>
</task>

</tasks>

<flagged_assumptions>
These edge-probe rows are `unclassified/unresolved` — surfaced explicitly (never silently dropped). Their parent requirements are already covered by the ROADMAP Phase-1 Success Criteria and this plan's tasks/must_haves:

- **DEPLOY-01 (unclassified):** No specific edge shape was inferable from the requirement text; the requirement itself ("free-hosted backend+Postgres, no cold-start, expiry checked vs July 17") is covered by Task 2 (provisioning) + Task 3 (health + no-sleep verification + trial-window recording). Assumption: Railway trial resource ceiling (1GB RAM, shared vCPU) is sufficient for FastAPI + APScheduler + a handful of concurrent judge sessions — unverified by load test (RESEARCH A4/Open Q2); mitigation is the documented Render+UptimeRobot fallback.
- **DEPLOY-03 (unclassified):** Covered by Task 3's dual-path reset verification. Assumption: Railway trial tier permits `railway run` / web-shell exec against the deployed service; if not, the token-guarded reset path is the sanctioned fallback (RESEARCH Open Q3).
</flagged_assumptions>

<assumption_delta_decision>
- **Noun:** deployment host provider (primary vs "fallback")
- **Decision:** add-alongside
- **Rationale:** Railway is provisioned as the single live primary (CONTEXT.md locks Railway primary; no-default-sleep is lower cold-start risk than Render). Render + UptimeRobot is kept as a documented-only fallback path (the repo-root Dockerfile pattern already covers both hosts) — NOT provisioned unless Railway shows resource strain in rehearsal.
- **Accepted debt:** the Render + UptimeRobot fallback path is documented but untested/unprovisioned until it is actually needed; standing it up mid-sprint would cost a fresh provisioning + keep-alive setup under deadline pressure.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> Railway backend | Untrusted callers reach /health and every API route over HTTPS |
| Railway backend -> Neon Postgres | DB credentials + TLS channel cross the host boundary |
| operator -> demo-reset path | Destructive re-seed must not be publicly callable |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure / Spoofing | CORS on backend/app/main.py | high | mitigate | Narrow allow_origins from "*" to the Vercel production origin (+ scoped *.vercel.app preview regex); verified by cross-origin curl in Task 3 |
| T-01-02 | Spoofing / Information Disclosure | Prod secrets (JWT_SECRET / FERNET_KEY) | high | mitigate | Generate fresh secrets, set only in the Railway dashboard, never commit or log; git-diff acceptance check confirms no secret literals in tracked files |
| T-01-03 | Elevation of Privilege / Tampering | Remote demo-reset path (DEPLOY-03) | high | mitigate | Prefer host-console exec (no public surface); if a code path is used, gate behind a static env-var bearer token, never logged/committed |
| T-01-04 | Information Disclosure | Neon TLS channel | medium | mitigate | asyncpg connect_args ssl=require; never disable/downgrade TLS; confirm neon.tech host |
| T-01-SC | Tampering | Backend has no new package installs this plan | low | accept | No npm/pip/cargo installs added here (deploy-config only); supply-chain gate handled in 01-02 where the frontend installs run |
</threat_model>

## Artifacts this phase produces

- **Files:** `backend.Dockerfile` (repo root, new)
- **Edited files:** `backend/app/main.py` (CORS allow_origins + allow_origin_regex), `backend/app/db.py` (`_engine_kwargs` ssl connect_arg)
- **Hosting resources:** Railway service (backend), Neon Postgres project — both free tier
- **Env vars (set in Railway, not committed):** DATABASE_URL, JWT_SECRET, FERNET_KEY, PROVIDER, SCORING_BACKEND, MONITOR_ENABLED (+ fee-schedule defaults as required), RAILWAY_DOCKERFILE_PATH
- **Live endpoints:** `https://<railway-app>/health` (badge + keep-alive target)
- **Operational path:** remote demo-reset (Railway console `make reset` or token-guarded path)

<verification>
- `curl $RAILWAY_URL/health` → 200 + expected JSON (status/app/env/provider/scoring_backend).
- Repo-root Docker build (or Railway build log) resolves `../rafid-engine` and completes `uv sync --frozen`.
- Cross-origin curl proves CORS allows the Vercel origin and not a foreign origin.
- Remote demo-reset executed once, DB re-seeded without error.
- No secret literals in any tracked file.
</verification>

<success_criteria>
The existing backend + rafid-engine runs live on Railway against Neon Postgres, reachable over HTTPS with a narrowed CORS allow-list, TLS-connected, no cold-start, and remotely resettable — with fresh uncommitted prod secrets and a documented (not provisioned) Render+UptimeRobot fallback.
</success_criteria>

<output>
Create `.planning/phases/01-live-bilingual-foundation/01-01-SUMMARY.md` when done — record the live Railway URL, the reset path that is live, trial-window margin vs July 17, and confirmation Neon is a neon.tech host.
</output>
