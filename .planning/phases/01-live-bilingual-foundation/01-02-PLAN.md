---
phase: 01-live-bilingual-foundation
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/next.config.ts
  - frontend/postcss.config.mjs
  - frontend/tsconfig.json
  - frontend/.env.local
  - frontend/.env.example
  - frontend/src/i18n/routing.ts
  - frontend/src/i18n/request.ts
  - frontend/src/middleware.ts
  - frontend/src/messages/ar.json
  - frontend/src/messages/en.json
  - frontend/src/lib/format.ts
autonomous: false
requirements: [FOUND-01, FOUND-05]
user_setup: []

must_haves:
  truths:
    - "Visiting the app with no locale prefix redirects to /ar (Arabic is the default locale); /ar renders dir=rtl lang=ar and /en renders dir=ltr lang=en via next-intl [locale] routing"
    - "The chosen locale persists across a refresh via the next-intl cookie (a returning visitor keeps their last locale over Accept-Language)"
    - "frontend/src/lib/format.ts is the single entry point for number/currency/date formatting; it uses ar-SA-u-nu-latn (Western digits in Arabic) and forces Gregorian for dates via ar-SA-u-ca-gregory-nu-latn"
    - "formatCurrency renders the currency symbol in the locale-deterministic position: Arabic as `<amount> ر.س`, English as `SAR <amount>` (ordering is defined, not incidental)"
    - "Every value formatted by lib/format.ts is intended to be wrapped by the caller in <bdi> so a mixed-direction Arabic run (adjacency of Latin digits inside RTL text) never visually reorders"
    - "lib/format.ts has defined behavior for empty/edge inputs: null/undefined returns a fixed placeholder (an em dash) and 0 returns a formatted zero — it never throws or renders 'NaN'/'Invalid Date'"
    - "NEXT_PUBLIC_API_URL is the only source of the backend base URL (D-04); .env.local sets it to http://localhost:8000 for dev and .env.example documents it — no host string is hardcoded anywhere"
  artifacts:
    - "frontend/ Next.js App Router project (Tailwind v4, TypeScript, src dir, @/* import alias)"
    - "frontend/src/i18n/routing.ts (locales ['ar','en'], defaultLocale 'ar', localePrefix 'always')"
    - "frontend/src/i18n/request.ts (next-intl getRequestConfig)"
    - "frontend/src/middleware.ts (next-intl middleware with the api/_next/_vercel/static matcher)"
    - "frontend/next.config.ts (next-intl plugin wrapper)"
    - "frontend/src/messages/ar.json + en.json (parallel key sets, Arabic default)"
    - "frontend/src/lib/format.ts (formatNumber, formatCurrency, formatDate)"
    - "frontend/.env.local + frontend/.env.example (NEXT_PUBLIC_API_URL)"
  key_links:
    - "next.config.ts MUST wrap the config with the next-intl plugin pointing at src/i18n/request.ts, or getTranslations/routing throw at runtime"
    - "middleware.ts matcher MUST exclude /api, /_next, /_vercel and files-with-extensions, or asset requests get locale-redirected and 404"
    - "format.ts date path MUST use ar-SA-u-ca-gregory (bare ar-SA defaults to the Islamic Umm al-Qura calendar — dates would silently mismatch the Gregorian backend)"
  prohibitions:
    - statement: "MUST NOT let the prohibited financing term (the Arabic and English words for the conventional lending charge) appear in any message catalog string — Murabaha framing only, per the hard product rule"
      status: unverified
    - statement: "MUST NOT compute or derive any score, fee, profit, or days-remaining value on the client — lib/format.ts only FORMATS server-provided values; no arithmetic on financial figures"
      status: unverified
    - statement: "MUST NOT read theme/locale from localStorage or sessionStorage before paint, and MUST NOT hardcode any backend host string — the API base is env-driven only (NEXT_PUBLIC_API_URL)"
      status: unverified
---

## Phase Goal

**As a** hackathon judge, **I want to** load Rafid and immediately get an Arabic-RTL-by-default bilingual experience with correct, bidi-safe number/currency/date formatting, **so that** the Arabic-first product reads as professional and every later screen inherits one correct formatting seam.

> Note (planner): user story derived from the ROADMAP Phase-1 Success Criteria (prose Goal line was not in user-story form). See 01-01 for the same note.

<objective>
Scaffold the greenfield `frontend/` Next.js App Router application, wire next-intl locale-prefixed routing (Arabic default, English toggle) with cookie persistence, and build the single central bidi-safe number/currency/date utility that every later Rafid screen formats through. Runs in Wave 1 in parallel with the backend deploy (01-01) — shares no files with `backend/`. This is the routing + formatting foundation; the visible token system, theming, and chrome come in 01-03/01-04 which depend on this.

Purpose: Establish the "scaffold once, never retrofit" i18n + formatting seam (FOUND-01, FOUND-05) before any screen exists.
Output: A running Next.js app that redirects to /ar, a bilingual routing config, message catalogs, and lib/format.ts.
</objective>

<execution_context>
@C:/Users/Saeed/Desktop/Amad Hackathon/Rawafid/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Saeed/Desktop/Amad Hackathon/Rawafid/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-bilingual-foundation/01-RESEARCH.md
@.planning/phases/01-live-bilingual-foundation/01-PATTERNS.md
@.planning/phases/01-live-bilingual-foundation/01-CONTEXT.md
@.planning/phases/01-live-bilingual-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js App Router + Tailwind v4 (installs only, pause for legitimacy gate)</name>
  <files>frontend/package.json, frontend/postcss.config.mjs, frontend/tsconfig.json (+ create-next-app generated scaffold under frontend/src/)</files>
  <read_first>
    - .planning/phases/01-live-bilingual-foundation/01-RESEARCH.md — Standard Stack (exact versions), Installation block, Package Legitimacy Audit
    - .planning/phases/01-live-bilingual-foundation/01-CONTEXT.md — D-01 (frontend/ sibling to backend/), D-04 (env-driven API URL)
  </read_first>
  <action>
    From the repo root, scaffold into `frontend/` with create-next-app using TypeScript, App Router, Tailwind, ESLint, the src directory, and the @/* import alias (the exact invocation is in RESEARCH.md Installation). This pins next (16.x), react/react-dom (19.x), tailwindcss (4.x) + @tailwindcss/postcss, and typescript per the audit. Do NOT yet install next-intl / next-themes — that install waits until after the Task 2 legitimacy checkpoint approves. Confirm frontend/ is a sibling of backend/ and rafid-engine/ (D-01); leave the Python directories untouched. Capture the generated package.json so the checkpoint can compare package names/versions to the Package Legitimacy Audit.
  </action>
  <verify>
    <automated>test -f frontend/package.json && grep -q '"next"' frontend/package.json && echo SCAFFOLD_OK</automated>
    <automated>test -f frontend/postcss.config.mjs && grep -q '@tailwindcss/postcss' frontend/postcss.config.mjs && echo TAILWIND_V4_OK</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` exists and lists next, react, react-dom, tailwindcss, typescript.
    - `frontend/postcss.config.mjs` references @tailwindcss/postcss (Tailwind v4 install path).
    - `frontend/src/` exists and the tsconfig import alias resolves @/* to src/*.
    - No files under backend/ or rafid-engine/ were modified.
  </acceptance_criteria>
  <done>The Next.js + Tailwind v4 project is scaffolded in frontend/ with the audited core packages, ready for the legitimacy gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package legitimacy gate (confirm audited names before further installs)</name>
  <action>Human confirms the installed and queued package names against the RESEARCH Package Legitimacy Audit before next-intl / next-themes / lucide-react are installed — the SUS-flagged packages are documented false positives but the gate is never auto-approved.</action>
  <what-built>create-next-app installed the core stack (next, react, react-dom, tailwindcss, typescript). RESEARCH flagged next / tailwindcss / typescript as SUS on the "too-new" recency heuristic — a known false-positive pattern for these extremely-high-download official packages, plus next-intl (also SUS false-positive) and next-themes (OK) are about to be installed.</what-built>
  <how-to-verify>
    1. Open frontend/package.json. Confirm the installed package NAMES exactly match the Package Legitimacy Audit rows in 01-RESEARCH.md: next, react, react-dom, tailwindcss, @tailwindcss/postcss, typescript — no typo-squat variants, no unexpected extra packages.
    2. Confirm the three packages queued for install are exactly `next-intl` (github.com/amannn/next-intl), `next-themes` (github.com/pacocoursey/next-themes), and `lucide-react` (github.com/lucide-icons/lucide — the UI-SPEC icon library, treated [ASSUMED] since it was not in the RESEARCH audit) — the audited/official names, not look-alikes.
    3. Spot-check on npmjs.com that these are the official packages (download counts in the millions, official org repos) if any name looks off.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the next-intl / next-themes / lucide-react install and configuration, or name any package that looks wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Install i18n libraries, wire locale routing + message catalogs, add central format utility + env</name>
  <files>frontend/src/i18n/routing.ts, frontend/src/i18n/request.ts, frontend/src/middleware.ts, frontend/next.config.ts, frontend/src/messages/ar.json, frontend/src/messages/en.json, frontend/src/lib/format.ts, frontend/.env.local, frontend/.env.example, frontend/package.json</files>
  <read_first>
    - .planning/phases/01-live-bilingual-foundation/01-PATTERNS.md — routing.ts, middleware.ts, and lib/format.ts sections (near-verbatim verified code); the format.ts anti-pattern note (bare ar-SA renders Hijri)
    - .planning/phases/01-live-bilingual-foundation/01-RESEARCH.md — Pattern 1 (routing + middleware + cookie), Pattern 4 (format utility), Recommended Project Structure
    - .planning/phases/01-live-bilingual-foundation/01-CONTEXT.md — D-08 (Arabic default, cookie persistence), D-13..D-16 (digits/currency/date/bidi rules)
    - RAFID_FRONTEND_HANDOFF.md — §0 (Murabaha framing, never the conventional-lending term), §1 (currency ر.س / SAR)
  </read_first>
  <action>
    Install next-intl, next-themes, and lucide-react (the UI-SPEC icon library — installed now under the approved legitimacy gate so 01-03 stays autonomous). Create src/i18n/routing.ts via defineRouting with locales ['ar','en'], defaultLocale 'ar', localePrefix 'always' (D-08). Create src/i18n/request.ts (getRequestConfig loading the matching messages catalog). Create src/middleware.ts using next-intl createMiddleware(routing) with the matcher that excludes /api, /_next, /_vercel and dotted files. Wrap frontend/next.config.ts with the next-intl plugin pointing at ./src/i18n/request.ts.

    Create src/messages/ar.json and src/messages/en.json with parallel key sets for the strings this phase and 01-04 need (app/brand name "رافد"/"Rafid", nav/toggle labels, hero headline + subheadline, primary CTA "كيف يعمل رافد؟"/"How Rafid works", the three how-it-works items, health-badge states جارٍ التحقق/متصل/غير متصل and Checking…/Live/Offline, and the demo disclaimer exactly per the UI-SPEC Copywriting Contract). All copy uses Murabaha framing; the conventional-lending charge term must never appear in either catalog.

    Create src/lib/format.ts as the single formatting entry point (D-16), following PATTERNS.md Pattern 4 exactly: formatNumber and formatCurrency use ar-SA-u-nu-latn for ar (Western digits in both locales, D-13) and en-US for en; formatCurrency returns `<amount> ر.س` for ar and `SAR <amount>` for en with 2 fraction digits and tabular-nums at call sites (D-14); formatDate uses ar-SA-u-ca-gregory-nu-latn for ar (Gregorian forced — bare ar-SA would silently render Hijri, D-15) and en-US for en with year/long-month/day. Add defined edge behavior: null/undefined input returns the em-dash placeholder "—" and does not throw; a 0 input formats as a real zero. The utility only FORMATS values — it performs no financial arithmetic. Callers wrap output in <bdi> (document this in a header comment describing intent by concept, without embedding any negative-grep literal).

    Create frontend/.env.local with NEXT_PUBLIC_API_URL=http://localhost:8000 (gitignored) and frontend/.env.example documenting NEXT_PUBLIC_API_URL (D-04) — no backend host is hardcoded in any source file.
  </action>
  <verify>
    <automated>test -f frontend/src/lib/format.ts && grep -q 'ar-SA-u-ca-gregory' frontend/src/lib/format.ts && echo GREGORIAN_OK</automated>
    <automated>grep -q 'ar-SA-u-nu-latn' frontend/src/lib/format.ts && echo LATN_DIGITS_OK</automated>
    <automated>test -f frontend/src/i18n/routing.ts && grep -q "defaultLocale: 'ar'" frontend/src/i18n/routing.ts && echo AR_DEFAULT_OK</automated>
    <automated>test -f frontend/src/messages/ar.json && test -f frontend/src/messages/en.json && node -e "const a=require('./frontend/src/messages/ar.json'),e=require('./frontend/src/messages/en.json');const ka=Object.keys(a).sort().join(),ke=Object.keys(e).sort().join();if(ka!==ke)process.exit(1)" && echo CATALOG_PARITY_OK</automated>
    <automated>cd frontend && npm run build >/dev/null 2>&1 && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/i18n/routing.ts` sets defaultLocale 'ar', locales ['ar','en'], localePrefix 'always'.
    - `frontend/next.config.ts` wraps config with the next-intl plugin referencing src/i18n/request.ts.
    - `frontend/src/lib/format.ts` uses ar-SA-u-nu-latn (numbers/currency) and ar-SA-u-ca-gregory-nu-latn (dates); formatCurrency yields `ر.س` for ar and `SAR` for en; null/undefined → "—", 0 → formatted zero.
    - `ar.json` and `en.json` have identical top-level key sets (catalog parity); `grep -c 'فائدة' frontend/src/messages/ar.json` returns 0 and `grep -ci 'interest' frontend/src/messages/en.json` returns 0 (Murabaha framing only).
    - `frontend/.env.example` documents NEXT_PUBLIC_API_URL; no source file hardcodes a backend host.
    - `npm run build` in frontend/ succeeds.
  </acceptance_criteria>
  <done>The app redirects to /ar, switches to /en with cookie persistence, message catalogs are at parity, and every future number/currency/date renders through one Gregorian-forced, Latin-digit, bidi-safe utility.</done>
</task>

</tasks>

<flagged_assumptions>
- **FOUND-01 (unclassified edge):** No specific edge shape was inferable from the requirement text; FOUND-01 (Arabic default, English toggle, locale-prefixed routes, copy parity) is covered by Task 3's routing config + catalog-parity check and by the render assertions in 01-04. Assumption: next-intl's cookie-over-Accept-Language priority is sufficient for "persisted across refresh" without extra cookie code (RESEARCH Pattern 1).
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> frontend build | Installed packages execute at build/dev time |
| browser -> NEXT_PUBLIC_API_URL | The public env var value is embedded in the client bundle |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (next, next-intl, next-themes, tailwindcss, typescript) | high | mitigate | Package Legitimacy Audit (RESEARCH) + the blocking-human checkpoint (Task 2) confirming exact names/versions before further installs; all SUS verdicts are documented false-positives |
| T-02-01 | Information Disclosure | NEXT_PUBLIC_API_URL in client bundle | low | accept | The API base URL is non-secret (public backend). No secret is placed in any NEXT_PUBLIC_ var; secrets stay server-side (01-01) |
| T-02-02 | Input Validation | locale segment value | low | mitigate | next-intl validates the locale against the fixed ['ar','en'] set; unknown locales 404/redirect, not rendered |
</threat_model>

## Artifacts this phase produces

- **Project:** `frontend/` (Next.js 16 App Router, Tailwind v4, TypeScript, src dir, @/* alias)
- **Config files:** `frontend/next.config.ts` (next-intl plugin), `frontend/postcss.config.mjs`, `frontend/tsconfig.json`
- **i18n:** `frontend/src/i18n/routing.ts`, `frontend/src/i18n/request.ts`, `frontend/src/middleware.ts`
- **Message catalogs:** `frontend/src/messages/ar.json`, `frontend/src/messages/en.json`
- **Utility (function exports):** `frontend/src/lib/format.ts` → `formatNumber`, `formatCurrency`, `formatDate`
- **Env:** `frontend/.env.local` (gitignored), `frontend/.env.example` — `NEXT_PUBLIC_API_URL`
- **Packages added:** next-intl, next-themes, lucide-react (+ create-next-app core stack)

<verification>
- `npm run build` in frontend/ succeeds.
- Routing: bare `/` redirects to `/ar`; `/ar` and `/en` both resolve (verified in dev / in 01-04 render assertions).
- Catalog parity check passes (ar.json and en.json key sets identical).
- format.ts uses the Gregorian + Latin-digit locale strings and defines null/0 behavior.
</verification>

<success_criteria>
The greenfield frontend runs, defaults to Arabic RTL routing with English toggle + cookie persistence, message catalogs are at parity with no prohibited term, and one central bidi-safe utility formats all numbers/currency/dates with Western digits and forced-Gregorian dates.
</success_criteria>

<output>
Create `.planning/phases/01-live-bilingual-foundation/01-02-SUMMARY.md` when done — record the Next.js/next-intl versions installed, the routing config, and the format.ts contract.
</output>
